<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      form {
        margin: 60px 500px;
      }
      form div {
        margin-top: 30px;
      }
      form input {
        width: 300px;
        padding: 10px;
      }
      form button {
        font-size: 15px;
        font-weight: 600;
        background-color: burlywood;
        border-radius: 20px;
        margin-top: 20px;
        padding: 20px 100px;
      }
    </style>
  </head>
  <body>
    <form action="/nations/<%= nation._id %>?_method=PATCH" method="POST">
      <caption>
        <h1>Edit Nations</h1>
      </caption>
      <div>
        <label for="">Name</label>
        <br />
        <input
          id="ip_name"
          type="text"
          name="name"
          placeholder="type name"
          value="<%= nation.name %>"
        />
      </div>
      <div>
        <label for="">Description</label>
        <br />
        <input
          id="ip_description"
          type="text"
          name="description"
          placeholder="type descrpition"
          value="<%= nation.description %>"
        />
      </div>
      <button type="submit">Edit Nation</button>
    </form>
  </body>
</html>
